---
name: python-yaml-frontmatter
description: Use `roadmap_lib`'s built-in frontmatter parser for `---` delimited frontmatter; no PyYAML dependency
artifact: guidelines/language/python/yaml-frontmatter.md
version: 1.0.0
---

## Worker Focus
Use `roadmap_lib`'s built-in frontmatter parser for `---` delimited frontmatter; no PyYAML dependency

## Verify
No `import yaml` or `PyYAML` dependency; frontmatter parsed via `roadmap_lib`; `---` delimiters handled correctly
