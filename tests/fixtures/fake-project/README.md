# Fake Project

A minimal project directory used as cwd during interview tests. The `createFakeProject()` function in the test harness creates temp copies of this with a `.git` directory so the interview skill can infer a project name.
